## 1. Component Updates

- [x] 1.1 Locate the Hero component implementation and identify the overlay and label rendering
- [x] 1.2 Update the default `overlayOpacity` to `0.3` when no value is provided
- [x] 1.3 Remove the lower-left page-name label from the Hero render output

## 2. Page Configuration

- [x] 2.1 Set the `/index` page Hero configuration to `overlayOpacity={0}`
- [x] 2.2 Verify other pages render correctly with the new default overlay

## 3. Visual QA

- [x] 3.1 Check `/index` hero appears fully clear (no dark overlay)
- [x] 3.2 Spot-check a few pages with Hero images for text readability

## Why

Implementation of the page /index, creating a clone of `https://grupoalfa.net/` as faithful as possible, to serve as the main entry point for the restored website.

## What Changes

- Update `/index.html` to fully replicate the content, layout, and behavior of the original homepage.
- Integrate links to all recently created sub-pages (`/proyectos`, `/contacto`, `/alfa-hunters`, etc.).
- Integrate links should point to the correct local files.
- Update navigation links to point to the correct local files.
- `index.html` should be the default page.
- Update `NewWeb/index.html` to fully replicate the content, layout, and behavior of the original homepage, downloading any missing assets.
- `NewWeb/index.html` should contains the same top banner picture as `https://grupoalfa.net/`.

## Capabilities

### New Capabilities
- `web-index`: Implementation of the main homepage with hero section, services overview, and navigation hub.

## Impact

- Modified page: `NewWeb/index.html`.

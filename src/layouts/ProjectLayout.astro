---
import BaseLayout from "./BaseLayout.astro";

const { title, bodyClass, footerStyle, activeNav } = Astro.props;
---

<BaseLayout
    title={title}
    bodyClass={bodyClass}
    footerStyle={footerStyle}
    activeNav={activeNav}
>
    <slot name="head" slot="head" />
    <slot />
</BaseLayout>
